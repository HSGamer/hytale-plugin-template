{
  "Group": "${pluginGroup}",
  "Name": "${project.name}",
  "Version": "${version}",
  "Description": "${project.description}",
  "Authors": [
    {
      "Name": "${authorName}",
      "Email": "${authorEmail}",
      "Url": "${authorUrl}"
    }
  ],
  "Website": "${project.url}",
  "ServerVersion": "*",
  "Dependencies": {},
  "OptionalDependencies": {},
  "DisabledByDefault": false,
  "IncludesAssetPack": false,
  "Main": "${mainClass}"
}
